import React from "react";
import Toolbar from "../components/Toolbar/Toolbar";

const Contacts = () => {
  return (
    <div>
      <Toolbar />
      <h1>Contacts page</h1>
    </div>
  );
};

export default Contacts;
